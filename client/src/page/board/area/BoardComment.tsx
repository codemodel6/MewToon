import styled from "styled-components";

const BoardCommentWrapper = styled.div`
  height: 100px;
  width: 100%;
`;

const BoardComment = () => {
  return <BoardCommentWrapper></BoardCommentWrapper>;
};

export default BoardComment;
